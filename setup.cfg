[flake8]
max-line-length = 100
extend-ignore = E203, W503, E402
exclude =
    .git,
    .venv,
    __pycache__,
    outputs,
    build,
    dist

[tool:pytest]
testpaths = tests
addopts = -q
